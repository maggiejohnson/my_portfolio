<div class="container">
    <div class="row centered mt mb">
        <div class="col-lg-8 col-lg-offset-2">
            <h1 class="blackness"><center><%= @title %></center></h1>
                <% @photos.each do |photo| %>
                    <img src="<%= photo.images.thumbnail.url %>" />
                <% end %>
        </div>
    </div>
</div>